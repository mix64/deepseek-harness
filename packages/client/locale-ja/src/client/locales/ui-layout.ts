import type { JaDictionaries } from './types.ts'

/** Japanese dictionaries for the `@deepseek-ai/dsh-client-ui-layout` namespaces. */
export const uiLayoutJa = {
  'shortcuts.layout': {
    'toggle': '左サイドバーの表示を切り替え',
  },
} satisfies JaDictionaries
